import {makeGrid} from '../helpers/helper'

const initial = {
  state:makeGrid(10,10)
} 

function gameReducer(state = initial, action){
  switch(action.type){
      
  }
}


export default gameReducer;